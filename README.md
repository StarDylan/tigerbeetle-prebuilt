# tigerbeetle

Tigerbeetle, but the rust client has everything pre-built. Super easy to add to projects now.

For 0.17.4, just add to `Cargo.toml`
```rust
tigerbeetle = { git = "https://github.com/StarDylan/tigerbeetle-prebuilt.git", tag = "0.17.4-prebuilt" }
```
